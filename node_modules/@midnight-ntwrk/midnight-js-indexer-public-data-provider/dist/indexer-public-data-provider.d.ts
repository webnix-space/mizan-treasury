import { type ContractAddress } from '@midnight-ntwrk/midnight-js-protocol/ledger';
import type { PublicDataProvider, SegmentStatus, TxStatus, UnshieldedBalances, UnshieldedUtxos } from '@midnight-ntwrk/midnight-js-types';
import * as ws from 'isomorphic-ws';
import { type ContractBalance, type RegularTransaction, type TransactionResult } from './gen/graphql';
export declare const isRegularTransaction: (tx: any) => tx is RegularTransaction & {
    hash: string;
    identifiers: string[];
};
export declare const toTxStatus: (transactionResult: TransactionResult) => TxStatus;
export declare const toSegmentStatus: (success: boolean) => SegmentStatus;
export declare const toSegmentStatusMap: (transactionResult: TransactionResult) => Map<number, SegmentStatus> | undefined;
export type IndexerUtxo = {
    owner: string;
    intentHash: string;
    tokenType: string;
    value: string;
};
export declare const toUnshieldedUtxos: (createdUtxo: readonly IndexerUtxo[], spentUtxo: readonly IndexerUtxo[]) => UnshieldedUtxos;
export declare const toUnshieldedBalances: (contractBalances: readonly ContractBalance[]) => UnshieldedBalances;
/**
 * Correlates a contract action at `contractAddress` with the transaction's
 * identifier at the same positional index. Throws {@link IndexerDataError}
 * when the deploy lacks an action for the address, when the corresponding
 * identifier slot is missing, or when the identifier is not a non-empty
 * string — all indicate that the indexer's contract-action / identifier
 * rows are out of sync.
 *
 * @internal Exported for unit testing the correlation in isolation.
 * Production callers should go through `PublicDataProvider.watchForDeployTxData`.
 */
export declare const correlateDeployTxId: (contractAddress: ContractAddress, contractActions: readonly {
    readonly address: string;
}[], identifiers: readonly string[]) => string;
/**
 * Constructs a {@link PublicDataProvider} based on an {@link ApolloClient}.
 *
 * @param queryURL The URL of a GraphQL server query endpoint.
 * @param subscriptionURL The URL of a GraphQL server subscription (websocket) endpoint.
 * @param webSocketImpl An optional websocket implementation for the Apollo client to use.
 *
 * TODO: Re-examine caching when 'ContractCall' and 'ContractDeploy' have transaction identifiers included.
 */
export declare const indexerPublicDataProvider: (queryURL: string, subscriptionURL: string, webSocketImpl?: typeof ws.WebSocket) => PublicDataProvider;
//# sourceMappingURL=indexer-public-data-provider.d.ts.map