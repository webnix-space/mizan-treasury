import { type CompiledContract } from '@midnight-ntwrk/midnight-js-protocol/compact-js';
import type { Contract } from '@midnight-ntwrk/midnight-js-protocol/compact-js/effect/Contract';
import type { CoinPublicKey, ContractAddress, EncPublicKey } from '@midnight-ntwrk/midnight-js-protocol/ledger';
import { type PrivateStateId } from '@midnight-ntwrk/midnight-js-types';
import { type CallResult } from './call';
import { type ContractProviders } from './contract-providers';
import * as Transaction from './transaction';
import type { FinalizedCallTxData } from './tx-model';
import type { CallTxOptions } from './unproven-call-tx';
/**
 * A type that lifts each circuit defined in a contract to a function that builds
 * and submits a call transaction.
 */
export type CircuitCallTxInterface<C extends Contract.Any> = {
    [PCK in Contract.ProvableCircuitId<C>]: {
        (...args: Contract.CircuitParameters<C, PCK>): Promise<FinalizedCallTxData<C, PCK>>;
        (txCtx: Transaction.TransactionContext<C, PCK>, ...args: Contract.CircuitParameters<C, PCK>): Promise<CallResult<C, PCK>>;
    };
};
/**
 * Creates a {@link CallTxOptions} object from various data.
 */
export declare const createCallTxOptions: <C extends Contract.Any, PCK extends Contract.ProvableCircuitId<C>>(compiledContract: CompiledContract.CompiledContract<C, any>, // eslint-disable-line @typescript-eslint/no-explicit-any
circuitId: PCK, contractAddress: ContractAddress, privateStateId: PrivateStateId | undefined, additionalCoinEncPublicKeyMappings: ReadonlyMap<CoinPublicKey, EncPublicKey> | undefined, args: Contract.CircuitParameters<C, PCK>) => CallTxOptions<C, PCK>;
/**
 * Creates a circuit call transaction interface for a contract.
 *
 * @param providers The providers to use to build transactions.
 * @param compiledContract The contract to use to execute circuits.
 * @param contractAddress The ledger address of the contract.
 * @param privateStateId The identifier of the state of the witnesses of the contract.
 */
export declare const createCircuitCallTxInterface: <C extends Contract.Any>(providers: ContractProviders<C>, compiledContract: CompiledContract.CompiledContract<C, any>, // eslint-disable-line @typescript-eslint/no-explicit-any
contractAddress: ContractAddress, privateStateId: PrivateStateId | undefined) => CircuitCallTxInterface<C>;
//# sourceMappingURL=tx-interfaces.d.ts.map